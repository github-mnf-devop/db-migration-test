apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flyway-db-migration
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/github-mnf-devop/db-migration.git'
    targetRevision: main
    path: flyway
  destination:
    server: https://kubernetes.default.svc
    namespace: db-migration-demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
